import java.util.Random;
import static LoggerProvider.getLogger;

/**
 * Define un Cliente, extiende Thread.
 * El cliente genera pedidos aleatorios.
 * @author mati
 */

public class Cliente extends Thread {

    int idCliente;
    static int cliente = 100;
    Random random;

    @Override
    public void run() {
        ProductManager.
        random = new Random();
    }
}
